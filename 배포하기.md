# 배포 — 지금 상태와 남은 일

**배포는 끝났습니다.** 현재 주소: `glowing-panda-32c01c.netlify.app`
Netlify 계정(Google 로그인)에 사이트가 들어와 있으므로 `Claim this site`는 해당 없습니다.

---

## 1. 지금 할 것 — 사이트 이름 바꾸기 (2분)

**게시글에 주소를 올리기 전에 해야 합니다. 이름을 바꾸면 URL도 같이 바뀌고, 옛 주소는 죽습니다.**

```
Netlify → 해당 프로젝트 → Project configuration → General
→ Project details → Change site name
```

이름 후보 (선점돼 있으면 다음 것으로):

| 후보 | 결과 주소 |
|---|---|
| `ai-posture-trainer` | ai-posture-trainer.netlify.app |
| `ai-form-trainer` | ai-form-trainer.netlify.app |
| `ai-trainer-home` | ai-trainer-home.netlify.app |
| `home-ai-trainer` | home-ai-trainer.netlify.app |

`ai-trainer` 같은 짧은 단어는 이미 선점돼 있을 가능성이 높습니다.
규칙: 영문 소문자·숫자·하이픈만. 전 세계에서 중복 불가.

> 주소에 작품명이 없어도 됩니다. 소개서·게시글·화면 안에는
> 작품명이 그대로 들어가 있으므로 심사에 문제되지 않습니다.

---

## 2. 코드를 고친 뒤 다시 올리기 (주소 유지)

**`app.netlify.com/drop`에 올리면 새 사이트·새 주소가 생깁니다. 쓰지 마세요.**

```
Netlify → 해당 프로젝트 → Deploys 탭
→ 페이지 가운데 "Drag and drop your project folder, zip file,
   or a single HTML file to deploy instantly" 영역에 index.html 드롭
```

- 주소 그대로, 몇 초 뒤 반영
- **방문자에게는 안 보입니다.** 이력은 님 계정에서만 보이고, 방문자는 최신 화면만 봅니다
- 그래서 **먼저 주소를 게시글에 올려두고, 마감까지 계속 고쳐도 됩니다**
- 반영이 안 보이면 강제 새로고침 (Windows `Ctrl+Shift+R` / Mac `Cmd+Shift+R`)

---

## 3. 접속 확인 (3분, 휴대폰으로)

- [ ] 페이지가 뜨는지
- [ ] 「카메라 없이 시연 보기」 → 8회 카운트되고 결과로 자동 스크롤되는지
- [ ] 「카메라 켜고 측정 시작」 → 권한 요청 창이 뜨는지
      (첫 실행 때 모델 5MB 다운로드로 몇 초 걸립니다)
- [ ] 종목 칩을 눌러 플랭크로 바뀌는지

---

## 안 될 때

| 증상 | 원인·조치 |
|---|---|
| 이름 변경이 거부됨 | 이미 선점된 이름입니다. 다른 후보로 |
| 페이지는 뜨는데 카메라가 안 켜짐 | 주소가 `https://`인지 확인. 주소창 자물쇠 → 카메라 허용 |
| "모델을 내려받지 못했습니다" | 회사·학교 와이파이가 CDN을 막는 경우. 모바일 데이터로 시도 |
| 첫 로딩이 느림 | 정상입니다. 모델 5MB를 한 번만 받고 이후엔 캐시됩니다 |
| 고친 게 반영이 안 됨 | 강제 새로고침. 그래도 안 되면 Deploys 탭에서 최신 배포가 Published인지 확인 |

## 대안 (Netlify가 막힐 때)
- **GitHub Pages**: 저장소에 `index.html` 올리고 Settings → Pages → Source를 main으로
- **Vercel**: https://vercel.com/new 에서 폴더 드래그
- **Cloudflare Pages**: https://pages.cloudflare.com 에서 직접 업로드

셋 다 무료이고 HTTPS가 자동으로 붙어 카메라가 동작합니다.
