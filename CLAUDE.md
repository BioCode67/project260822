# CLAUDE.md — 되감기(REWIND) 프로젝트 인계 문서

> 이 파일을 저장소 루트에 두면 Claude Code가 세션 시작 시 자동으로 읽습니다.
> 작성 시점: 2026-08-22. 공모전 마감 당일.

---

## 0. 지금 상황 요약 (제일 먼저 읽을 것)

제1회 **일상뒤집기 AI 어플 아이디어 공모전** (주최: 홈그라운드) 출품 작업 중.

- **마감: 2026-08-22(토) 23:59** — 오늘이다. 남은 시간을 항상 먼저 계산할 것.
- 제출처: homegle.co.kr → 게시판 → 자유게시판 → 공모전 참여 (게시글 1개)
- 필수 제출물 2종은 **이미 완성되어 있다.** 아래 "현재 완료 상태" 참조.
- 지금부터의 작업은 전부 **추가 개선**이다. 동작하는 것을 깨뜨리면 손해가 크다.

### 절대 규칙
1. `index.html`은 **단일 파일로 동작해야 한다.** Netlify Drop에 파일 하나를 드래그해 배포한다.
2. **백엔드 없음. API 키 없음.** 모든 계산은 브라우저 안에서 끝난다. 이게 이 서비스의 핵심 가치이자 심사 차별점이다.
3. **카메라 없이도 전체 흐름이 동작해야 한다.** 심사위원이 웹캠 권한을 거부할 수 있다. MediaPipe는 반드시 동적 `import()`로 지연 로드한다. 정적 import를 쓰면 CDN 차단 시 시연 모드까지 죽는다. (한 번 겪은 버그다.)
4. 수치·통계는 **검증한 것만** 쓴다. 출처와 연도를 명시한다.

---

## 1. 작품 개요

**이름:** 되감기 (REWIND)
**한 줄 요약:** 매일 홈트 자세가 맞는지 확인하는 게 너무 귀찮다 → AI로 무너진 순간만 잡아 되돌려 보여주는 되감기
**핵심 문장:** 거울은 '지금'만 보여줍니다. 되감기는 '무너진 순간'을 남깁니다.

웹캠 앞에서 스쿼트를 하면 MediaPipe Pose Landmarker가 관절 33점을 매 프레임 추정하고, 세 각도를 계산한다. 기준을 넘긴 프레임 중 **가장 심하게 무너진 한 장**을 회차당 하나씩 저장한다. 세트가 끝나면 "8회차 · 상체 64° · 깊이 98°" 형태의 증거가 남는다.

**포지셔닝:** "AI가 알아서 판단"하지 않는다. 무엇을 어떻게 쟀는지 화면에 숫자로 띄운다. 경쟁작 대부분이 하드코딩된 시나리오 재생인 상황에서, **실제로 브라우저에서 추론이 도는 것**이 유일한 기술 차별점이다.

---

## 2. 현재 완료 상태

| 항목 | 상태 | 파일 |
|---|---|---|
| 프로토타입 (단일 HTML) | **완료·검증됨** | `index.html` |
| 사업 소개서 5장 JPEG | **완료 · 2026-08-22 새 팔레트로 재생성** | `되감기_소개서_{1..5}of5.jpg` |
| 소개서 원본 (재생성용) | 완료 | `deck.html` + `a_live.png`, `a_panel.png`, `a_shots.png` |
| 소개서 재생성 방법 | 아래 6-D 참조 | Playwright 1240×1754 |
| 게시글 제목·본문 | 완료 (복사용) | `되감기_제출키트_김주형.md` |
| 회귀 검증 스크립트 | 완료 | `verify.py` |
| 게임 모드 A-1 (깊이 게이지 챌린지) | **완료·검증됨** | `index.html` |
| 인식 신뢰도 게이트 · 촬영 가이드 | **완료·검증됨** | `index.html` |
| 화면 위 각도 시각화(관절점·호·수직 기준선) | **완료·검증됨** | `index.html` |
| 세트 타임라인 · 코칭 한 줄 · 캡처 확대 | **완료·검증됨** | `index.html` |
| 오픈소스 출처 섹션 | 완료 | `index.html` |
| 세트 리포트 · 결과 PNG 저장 | **완료·검증됨** | `index.html` |
| 지난 세트 비교 (localStorage) | **완료·검증됨** | `index.html` |
| 한글 타이포그래피 정비 | 완료 | `index.html` |
| 종목 확장 7종 (스쿼트·푸시업·런지·벤치프레스·점핑잭·플랭크·목 스트레칭) | **완료·검증됨** | `index.html` |
| 가동범위·피로도 지표 | **완료·검증됨** | `index.html` |
| 디자인 정리 (타입 스케일·선 감축) | **완료·검증됨** | `index.html` |
| 대기 화면 시작 3단계·뷰파인더 · 모바일 헤더 수정 | **완료·검증됨** | `index.html` |
| 게임 모드 A-2 콤보 · A-3 템포 | **완료·검증됨** | `index.html` |
| 버티기(hold) 종목 모드 | **완료·검증됨** | `index.html` |
| 카메라 경로 실제 통합 검증 | 완료 | `index.html` |
| 유튜브 3분 스크립트 | 완료 (촬영 전) | 같은 파일 |
| SNS 인증 문구 | 완료 | 같은 파일 |
| Netlify 배포 | **미완** | — |
| 유튜브 영상 촬영·업로드 | **미완** | — |
| SNS 인증 3종 | **미완** | — |

### 검증된 동작 (데모 모드, 네트워크 차단 상태에서 확인)
- 8회 카운트 정상
- 깊이 도달 6/8 (후반 피로로 얕아지는 시나리오)
- 경고 캡처 4장 (5·6·7·8회차)
- 깊이 챌린지 성공 3/8 (1·2·3회차) — 아래 A-1 참조
- JS 런타임 오류 없음 (`file://`에서 폰트 CDN 2건이 막히는 건 정상. 계측과 무관)

---

## 3. 기술 구조

### 의존성
- **MediaPipe Tasks Vision 0.10.14** (Apache-2.0), jsDelivr CDN에서 동적 로드
  - 모델: `pose_landmarker_lite.task` (float16), Google 호스팅, 약 5MB
- **Pretendard** (본문), **JetBrains Mono** (수치) — CDN
- 그 외 의존성 없음. 빌드 단계 없음.

### 계측 로직 (`metrics()`)
좌표는 MediaPipe 정규화 좌표(0~1). 랜드마크 인덱스:
`11/12` 어깨, `23/24` 고관절, `25/26` 무릎, `27/28` 발목, `0` 코

| 지표 | 계산 | 임계값 |
|---|---|---|
| 무릎 각도 | 고관절–무릎–발목 3점 사잇각. 좌우 중 **작은 쪽** 채택 | ≤ 90° 깊이 인정 |
| 상체 기울기 | 어깨중점–고관절중점 벡터가 수직에서 벗어난 각 `atan2(dx, -dy)` | > 55° 경고 |
| 좌우 비대칭 | \|좌골반.y − 우골반.y\| ÷ 어깨너비 | > 8% 경고 |

### 횟수 상태머신 (`tick()`)
```
up --(무릎 ≤ 100°)--> down --(무릎 ≥ 158°)--> up  [reps++]
```
- `down` 구간에서 `minKnee` 갱신 → 회차 종료 시 `≤ 90°`면 `deepReps++`
- `down` 구간에서 `severity = max(상체/55, 비대칭/8%)` 를 추적.
  1을 넘고 기존 최댓값보다 크면 `keepFrame()`으로 캔버스 스냅샷 보관.
- 회차 종료 시 `commitShot()`이 보관된 스냅샷을 `toDataURL`로 확정.
  **회차당 최대 1장, 전체 최대 8장.**
- `toDataURL`은 비싸므로 매 프레임 호출하지 말 것. 반드시 회차 종료 시 1회만.

### 시연 모드 (`synth()`)
카메라 없이 동작시키기 위해 **2관절 링크 모델**로 관절 좌표를 합성한다.
합성 좌표를 실제와 **동일한** `metrics()` → `tick()` → `draw()` 파이프라인에 통과시킨다.
(가짜 결과를 출력하는 게 아니라, 진짜 계산기에 합성 입력을 넣는 구조다. 이 점을 소개서에도 명시했다.)

파라미터:
```js
d    = 0.5 - 0.5*cos(t)        // 0=기립, 1=최저점
f    = min(1, repIdx/8)        // 누적 피로
fade = 1 - 0.30*f              // 피로할수록 덜 앉는다
a    = 30° * d * fade          // 정강이 경사
b    = 80° * d * fade          // 대퇴 경사
phi  = 8° + d*(30° + 24°*f)    // 상체 전방 기울기
asym = 0.005 * d * f           // 골반 좌우 높이차
```
세그먼트 길이: 정강이 0.25, 대퇴 0.25, 상체 0.32, 어깨너비 0.06, 발목 y=0.95

**이 상수들은 튜닝된 값이다.** 바꾸면 8회/6회 깊이/4장 캡처 시나리오가 깨진다.
바꿀 경우 반드시 아래 검증 스크립트를 다시 돌릴 것.

---

## 4. 검증 방법

```bash
pip install playwright --break-system-packages && playwright install chromium
python3 verify.py index.html
```

`verify.py`가 저장소에 있다. 헤드리스 크로미움으로 시연 모드를 한 세트 돌리고 아래를 확인한다.
`index.html`을 건드렸으면 **반드시 이걸 통과시키고 커밋할 것.**

```
[스쿼트]      구간 8 · 달성 6 / 8 · 캡처 4
[푸시업]      구간 8 · 달성 4 / 8 · 캡처 2
[런지]        구간 8 · 달성 6 / 8 · 캡처 3
[벤치프레스]  구간 8 · 달성 6 / 8 · 캡처 3
[점핑잭]      구간 8 · 달성 4 / 8 · 캡처 2
[플랭크]      구간 3 · 달성 1 / 3 · 캡처 2
[목 스트레칭] 구간 3 · 달성 2 / 3 · 캡처 2
errors    none     (pageerror = JS 런타임 오류)
chal      참고값 · 합/불 판정 대상 아님
```

`verify.py`가 두 종목을 차례로 돌린다. 종목을 추가하면 `CASES`에 한 줄 넣으면 된다.

- 합/불은 위 네 줄로만 판정한다. 폰트 CDN 차단(`ERR_TUNNEL_CONNECTION_FAILED` 등)은
  리소스 로드 실패이지 JS 오류가 아니므로 참고 줄로만 찍고 넘어간다.
- 챌린지 점수를 판정에서 뺀 이유: 시연 모드는 프레임당 `demoT += 0.045`로 도는 **프레임 구동**이라
  버틴 시간이 그 기기의 프레임레이트에 따라 달라진다. 60fps에서 3/8이 나온다.

`file://`로 열면 **카메라는 동작하지 않는다**(브라우저 보안 정책). 로컬에서 카메라를 테스트하려면
`python3 -m http.server`로 `http://localhost`에서 열거나, Netlify에 올려 HTTPS로 확인한다.

---

## 5. 배포

1. `index.html` 단일 파일 준비
2. https://app.netlify.com/drop 에 드래그
3. 발급된 URL로 **다른 기기(휴대폰)에서** 접속해 확인
4. 첫 접속 시 모델 5MB 다운로드로 몇 초 걸린다 — 로딩 표시가 뜨는지 확인

---

## 6. 추가 개선 후보 (우선순위 순)

> 마감까지 남은 시간을 먼저 계산하고, **필수 미완 항목(배포·영상·SNS)을 끝낸 뒤에만** 손댈 것.

### A. 게임 모드 (사용자 요청, 가치 높음)
심사 기준상 "창의성 25%"에 직접 기여한다. 다만 **"정직한 계측"이라는 포지셔닝을 훼손하지 않는 형태**여야 한다.
계측을 게임으로 바꾸는 게 아니라, 계측 위에 얹는다.

제안 스코프 (작은 것부터):
1. ~~**깊이 게이지 챌린지**~~ — **2026-08-22 구현 완료.** 아래 "A-1 구현 메모" 참조
2. ~~**콤보 시스템**~~ — **2026-08-22 구현 완료.** 경고 없이 + 챌린지 성공인 회차만 이어진다.
   무너지면 0으로 끊긴다. 3연속부터 시안, 5연속부터 금색으로 바뀐다
3. ~~**템포 모드**~~ — **2026-08-22 구현 완료.** WebAudio 메트로놈이 2초 간격으로
   내려가기(높은 음)·올라오기(낮은 음)를 치고, 화면 가운데 링이 함께 퍼진다.
   하강 시간(**기립 마지막 순간 → 최저점**)을 실측해 리포트에 평균으로 남긴다.
   시연 스쿼트에서 0.80초가 나온다(목표 2.0초보다 빠름)

**소리는 기본 꺼짐이다.** 심사 환경에서 갑자기 소리가 나면 곤란하다.
`소리 켜기`를 누르거나 템포를 켜면 함께 켜진다. 성공은 높은 음, 콤보가 끊기면 낮은 음.
`AudioContext`는 첫 클릭 뒤에 만들고 `suspended`면 `resume()`한다(브라우저 자동재생 정책).

**하지 말 것:** 아바타·포인트 상점·리더보드(서버 필요) 같은 무거운 게임화. 시간도 없고 프라이버시 서사와 충돌한다.

#### A-1 구현 메모 (깊이 게이지 챌린지)

계측 패널 안, 세 게이지 아래에 `DEPTH CHALLENGE` 블록으로 들어갔다.

- 무릎 각도로 채워지는 트랙(180°→0%, 70°→100%)과 90° 지점의 목표선.
- `state==='down'`이면서 무릎 `≤ 90°`인 동안 `performance.now()` 델타를 적산한다.
  **0.5초(`CH.HOLD`)** 채우면 그 회차 성공 — 트랙이 초록으로 바뀐다.
  존을 벗어나면 0부터 다시 (성공한 뒤에는 유지). 프레임 델타는 120ms로 상한을 둬
  탭 전환 뒤 한 번에 채워지는 걸 막았다.
- 실패하면 트랙이 주황으로 바뀌며 흔들리고, **버틴 시간을 숫자로 적는다**
  ("4회차 실패 · 90° 아래에 머문 시간 0.43초"). 90° 아래로 못 갔으면 최저각을 대신 적는다.
  `prefers-reduced-motion`에서는 흔들림이 꺼진다.
- 우측 상단 점수는 `성공 / 전체 회차`. `세트 초기화` 옆의 `챌린지 끄기`로 껐다 켤 수 있다
  (계측만 보고 싶은 심사위원용). 껐을 때도 계측은 그대로 돈다.

**지킨 원칙 — 손대지 않은 것:**
- `metrics()` / `tick()` 상태머신 / `TH` 임계값 / `synth()` 상수: 전부 그대로다.
  챌린지는 `tick()`이 이미 계산한 `m.knee`와 `state`를 **읽기만** 한다.
- **캔버스에는 아무것도 그리지 않는다.** 경고 캡처(`keepFrame`)가 게임 그래픽으로
  오염되면 "증거"가 증거가 아니게 된다. 챌린지는 DOM에만 산다.
- 그래서 8회 / 깊이 6-8 / 캡처 4장 시나리오가 그대로 유지된다 (`verify.py` 통과 확인).

**깊이 도달(6/8)과 챌린지(3/8)가 다른 이유:** 깊이 도달은 *스쳐 지나간 최저각*이
90° 이하인지를 보고, 챌린지는 *90° 아래에 머문 시간*을 본다. 4회차는 최저 82°로
깊이는 인정되지만 0.43초밖에 못 버텨 챌린지는 실패다. 이 차이가 화면에 숫자로 설명돼 있다.

### B-0. 클래스 이름 충돌 — 두 번 밟았다
`index.html`은 단일 파일이라 CSS 이름공간이 하나다. 실제로 두 번 사고가 났다.
- `.tl` — 화면 좌상단 코너 라벨(`.corner.tl`)이 이미 쓰고 있었다. 세트 타임라인에 같은 이름을 붙였다가
  코너 라벨에 `margin-bottom:42px`가 먹었다. → `.tline`으로 변경
- `.hold` — 챌린지 진행바(`height:4px`)가 이미 쓰고 있었다. 종목 칩에 붙였더니
  버튼 높이가 38px에서 20px로 찌그러졌다. → `.timed`로 변경

**새 클래스를 붙이기 전에 `grep -n "\.클래스이름" index.html`로 먼저 확인할 것.**

### B-0-b. padding 축약형이 `.wrap`을 덮어썼다 (2026-08-22 7차)
`.hd`는 `class="wrap hd"`로 두 클래스를 함께 쓴다. `.wrap{padding:0 22px}`인데
`.hd{padding:14px 0}`처럼 **축약형**을 쓰면 좌우 22px까지 통째로 0이 된다.
같은 특이도라 뒤에 온 `.hd`가 이긴다. 넓은 화면에서는 `max-width:1160px`가 가운데 정렬돼
티가 안 나다가, 360~390px 화면에서 **로고가 헤더 밖으로 잘려 나갔다.**
→ `padding-top`/`padding-bottom`만 지정하도록 고쳤다.

**여러 클래스를 겹쳐 쓰는 요소에는 축약형 대신 방향별 속성을 쓸 것.**

### B-0-c. 대기 화면은 비율을 고정하면 안 된다
`.screen`은 카메라 비율 4:3이다. 대기 중에는 영상이 없어 데스크톱에서 660px짜리
빈 검은 상자가 됐다. `.screen.idle`로 대기 때만 `aspect-ratio:auto`를 주고
안내 내용 높이를 따라가게 했다(`min-height:min(52vw,420px)`, 900px 이하는 0).
**시작하면 반드시 4:3으로 되돌려야 한다** — 캔버스가 `object-fit:cover`라
비율이 어긋나면 스켈레톤의 머리·발이 잘린다. `setIdle(false)`가 그 역할이다.

또한 `ph.innerHTML`을 로딩·에러 문구가 덮어쓴 뒤 되돌리지 않아, 대기 상태로 복귀하면
`$('phHint')`가 `null`이라 `applyEx()`가 죽었다. `PH_HTML` 보관 + `phReset()`으로 복원한다.

### B. UI/UX 개선
현재 디자인은 "청사진(blueprint)" 콘셉트다 — 짙은 남색 배경, 격자, 시안 계측선, 주황 경고.
각도 측정이라는 주제에서 나온 방향이므로 유지할 것. 색상 토큰은 `index.html`의 `:root`에 있다.

### C. 종목 확장 — **2026-08-22 푸시업까지 구현 완료**
`EX` 등록부에 종목을 정의한다. 각 종목은 `TH`(임계값) · `metrics()` · `synth()` · 라벨만 갖는다.

**핵심 설계:** `metrics()`가 돌려주는 key(`knee`/`torso`/`tilt`)를 종목이 달라져도 **그대로 유지**했다.
그래서 `tick()` 상태머신 · `updatePanel()` · `commitShot()` · `recordRep()` · 리포트 · 타임라인이
한 줄도 바뀌지 않고 재사용된다. 바뀌는 것은 *무엇을 재는가*와 임계값, 라벨뿐이다.
소개서 5장의 "종목마다 재는 각도만 바꾸면 되므로 확장 비용이 낮습니다"가 이제 실제 코드다.

| | 주각도 (`knee`) | 보조 (`torso`) | 좌우 (`tilt`) | 기립/하강 |
|---|---|---|---|---|
| 스쿼트 | 고관절–무릎–발목 ≤90° | 상체 수직 이탈 >55° | 골반 높이차 >8% | 158° / 100° |
| 푸시업 | 어깨–팔꿈치–손목 ≤90° | 어깨–고관절–발목 일직선 이탈 >15° | 어깨 높이차 >8% | 158° / 110° |
| 런지 | 고관절–무릎–발목 ≤95° | 상체 수직 이탈 >25° | 골반 높이차 >8% | 158° / 110° |
| 플랭크 | 어깨–고관절–발목 이탈 ≤12° | 무릎 굽힘 >15° | 어깨 높이차 >8% | 18° / 12° (이력 6°) |
| 벤치프레스 | 어깨–팔꿈치–손목 ≤85° (**덜 내려간 쪽**) | 바 기울기 >5° | 팔꿈치 높이차 >8% | 158° / 110° |
| 점핑잭 | 몸통 연장선~팔 ≤30° | 상체 수직 이탈 >15° | 어깨 높이차 >8% | 140° / 60° |

**벤치프레스에서 배운 것 — 시점이 맞아야 잴 수 있다.**
처음엔 측면 시점으로 만들었는데 팔이 카메라 방향으로 움직여 투영각이 뭉개졌다
(전완 수직 이탈이 1회차부터 40°). **발치 쪽 시점**으로 바꾸니 두 팔이 모두 보여
가동범위와 바 기울기를 함께 잴 수 있게 됐다. 촬영 뷰 안내(`hint`)도 종목별로 다르다.
주각도는 `Math.max`(덜 내려간 쪽) — 세트의 질은 약한 쪽이 결정하기 때문이다.

### 디자인 토큰 (2026-08-22 5차)
기능이 쌓이면서 글자 크기가 11~17px 사이에 열 가지가 넘었고 상자와 선이 과했다.
`--t-xs/sm/md/lg/xl` 여섯 단으로 줄이고, 게이지 사이 점선 구분자를 없애 간격으로 나눴다.
테두리는 `--line-soft`로 옅히고 카드 배경은 `--tint` 계열 면으로 바꿨다.
격자 배경도 `.10 → .055`로 낮췄다. 게이지 설명은 "재는 곳 · 판정 기준" 한 줄 형식으로 통일.
| 목 스트레칭 | 귀–어깨–고관절 ≤158° | 상체 따라 기울기 >12° | 어깨 올라감 >8% | 164° / 158° (이력 6°) |

**점핑잭에서 배운 것 — 화면에 그린 각과 표시한 숫자는 반드시 같아야 한다.**
손목–어깨–고관절 사잇각은 팔을 올릴수록 *커진다*. 다른 종목과 방향이 반대라
`180 − 사잇각`("머리 위까지 남은 각")으로 돌려놨는데, 처음엔 호는 사잇각(69°)을 그리고
라벨은 111°를 찍었다. 정직한 계측이라는 포지셔닝에 정면으로 어긋난다.
`viz:'upGap'`을 만들어 **몸통 연장선에서 팔까지의 각을 직접 그리도록** 고쳤다. 이제 둘이 같다.

**런지가 보여 주는 것:** 스쿼트와 *재는 곳이 같다*(고관절–무릎–발목). 다른 건 임계값뿐이다 —
상체 기준이 55°에서 25°로 내려간다. 같은 각도라도 종목에 따라 좋은 자세가 다르다.

**목 스트레칭이 보여 주는 것:** 스트레칭도 같은 문법으로 잰다. 귀–어깨–고관절 사잇각은
똑바로 서면 180°, 옆으로 기울일수록 작아진다 — 무릎 각도와 완전히 같은 방향이라
상태머신·게이지·리포트가 그대로 돌아간다. 경고는 *상체가 같이 기우는 것*과
*어깨가 딸려 올라가는 것*, 스트레칭이 헛수고가 되는 두 가지 실수다.

### 버티기(hold) 모드 — 2026-08-22 4차
`kind:'hold'` 종목은 횟수가 아니라 **기준 안에 머문 시간**을 잰다.
놀랍게도 `tick()` 상태머신은 한 줄도 안 바꿨다 — **기준 안에 들어가면 `down`, 벗어나면 `up`**으로 보면
'한 구간'이 곧 '한 회차'가 된다. `DOWN`=12°, `STAND`=18°로 **6°의 이력(hysteresis)**만 주면
경계에서 떨지 않는다.

- 큰 숫자(`#repN`)는 회차 대신 **현재 버틴 시간(초)** — `chFrame()`이 갱신한다
- 목표 달성 판정은 `chBest >= 목표시간`(스쿼트는 `minKnee <= 90°`)
- 진행바용 `chHold`(목표에서 잘림)와 기록용 `chRaw`(안 잘림)를 분리했다.
  안 그러면 타임라인에 "7초"라고만 찍혀 실제로 8.5초 버틴 게 안 보인다
- **시연은 실시간 구동**이다. `demoT += dt/1000*2.7`. 횟수 종목의 프레임 구동
  (`demoT += 0.045`)은 검증된 값이라 그대로 뒀지만, 버티기는 '몇 초'가 결과 자체라
  프레임레이트에 흔들리면 안 된다. (헤드리스는 60fps, 다른 환경은 95fps까지 나왔다)

**`synthPlank()`에서 실제로 밟은 함정 두 개:**
1. 처짐 거리를 각도로 되돌릴 때 **고관절 양쪽 각이 더해진다**는 걸 빼먹어 실제 이탈각이
   의도의 1.83배로 나왔다. `sag = dev / (1/(0.45·BODY) + 1/(0.55·BODY))`로 환산해야 맞다
2. 진폭 `A`는 최대 이탈각(3+A)을 정한다. **`STAND`(18°)를 못 넘으면 구간이 영원히 안 끝난다.**
   A=14로 잡았다가 시연이 멈췄다

시연 시나리오: **8.5초 달성 → 6.5초(비대칭 10.8%) → 5.4초(비대칭 10.7%)**, 목표 7초 기준 1/3.

푸시업 시연은 **8회 / 깊이 4 / 캡처 2장**이다(`verify.py`가 함께 검증한다).
`synthPush()`는 발끝을 축으로 몸이 회전하고 손은 바닥에 고정된 2링크 역기구학 모델이다.
피로하면 `fade`로 덜 내려가고 `sag`로 허리가 처진다.

다음 종목(런지·플랭크)도 `EX`에 한 항목 추가 + `verify.py`의 `CASES`에 한 줄이면 된다.

**종목별로 갈아 끼우는 것 목록** (새 종목을 넣을 때 빠뜨리기 쉬운 부분):
`TH` · `metrics` · `synth` · `secMax`(보조 게이지 스케일) · `g1~g3`(게이지 라벨·설명) ·
`chal`(챌린지 제목·설명) · `cue`(코칭 문구) · `sec`/`tiltL`(경고 태그 약칭) ·
`joint`(타임라인·리포트·PNG에 쓰는 관절 이름) · `cnt`(패널 제목) · `arc`/`ref`(화면 위 시각화) · `name`/`hint`.

---

## 6-B. 2026-08-22 2차 개선 (인식 신뢰도 · UI/UX)

### 실제 인식 검증 (헤드리스 크로미움 + 실사진)
jsDelivr가 이 개발 환경에서 막혀 있어 npm에서 `@mediapipe/tasks-vision@0.10.14`를 받아
로컬 서버로 띄우고, 실제 모델(`pose_landmarker_lite`)로 **사람 사진 2장**을 추론해 확인했다.

| 입력 | knee | torso | tilt | shoulderW/torsoLen | 판정 |
|---|---|---|---|---|---|
| 정면 전신 (직립) | 173° | 3° | 1.3% | 0.99 (정면) | 정상 |
| 비스듬 (한쪽 무릎 굽힘) | 107° | 1° | 3.5% | 0.39 (비스듬) | 정상 |
| 정면 전신 · 발목 잘림 | 172° | — | — | — | **게이트가 차단** (visibility 0.37) |
| 비스듬 · 하단 40% 잘림 | **177°(오판)** | — | — | — | **게이트가 차단** (visibility 0.05) |

마지막 줄이 이 게이트를 넣은 이유다. 다리가 화면 밖으로 나가면 MediaPipe는 좌표를 그래도 내놓는데
그 값이 177°(선 자세)로 읽혀 **가짜 회차가 세어진다.** 이제 그런 프레임은 세지 않는다.

GPU 델리게이트는 `--disable-gpu` 환경에서도 열렸지만(내부적으로 소프트웨어 폴백),
안 되는 기기를 대비해 **GPU 실패 시 CPU로 자동 전환**을 넣었다. 현재 델리게이트는 화면 좌상단에 찍힌다.

### 넣은 것
- **인식 게이트(`quality()`)** — 핵심 관절 8개의 `visibility` 최솟값이 0.5 미만이거나, 관절이 화면 밖으로
  나갔거나, 너무 멀면 **`tick()`을 부르지 않는다.** 화면에는 계속 그려 주고 왜 멈췄는지 자막으로 적는다.
  **카메라 모드에만 적용**했다. `synth()`는 `visibility:1`을 주므로 시연 모드는 애초에 영향이 없다.
- **촬영 뷰 표시** — `shoulderW / torsoLen`로 정면(>0.55)·비스듬(0.25~0.55)·측면(<0.25)을 구분해
  지금 무엇이 계측되는지 알린다. **순수 측면에서는 좌우 비대칭이 계측 불가**라는 걸 밝힌다
  (임계값·경고 로직은 그대로 두고 표시만 한다 — 건드리면 캡처 4장 시나리오가 깨진다).
- **화면 위 각도 시각화** — 관절점을 `visibility`에 따라 진하기를 달리해 찍고, 무릎 사잇각을 **호**로 그리고,
  고관절에 **수직 기준선(점선)**을 세워 상체 각을 호로 표시한다. 캡처 이미지에도 그대로 남아 증거가 세진다.
- **세트 타임라인** — 회차별 최저 무릎각 + 경고 사유를 8칸 스트립으로. 소개서 1장의 도해와 같은 모양.
- **코칭 한 줄(`cue()`)** — 방금 뜬 숫자가 무슨 뜻인지 한 줄로 옮겨 적는다. 판정이 아니라 번역이다.
  `tick()`의 상태 전이 **뒤에** 호출해야 회차 종료 시점 문구가 맞는다(앞에서 부르면 'down' 문구가 남는다).
- **캡처 확대(라이트박스)**, 실패 원인별 카메라 에러 메시지, 15초 비활동 안내, `:focus-visible`,
  `aria-live`, og 메타.

### 참고한 자료
LearnOpenCV, "AI Fitness Trainer using MediaPipe" (Kunal Dawn, 2022).
수직 기준선 각도, 정면 오프셋 경고(OFFSET_THRESH), 15초 비활동 리셋 개념을 참고했다.
원문은 Python·OpenCV·Streamlit 데스크톱 앱이라 **코드는 가져오지 않았고**, 개념만 이 프로젝트 구조에
맞게 직접 구현했다. 페이지 하단 `OPEN SOURCE` 섹션에 이 사실을 명시해 뒀다(유의사항 01 대비).

### 확인 필요 — 소개서 1장과 앱의 상체 각도가 다르다
소개서 1장 스트립은 5~8회차 상체를 **56·59·61·64°**로 적었는데, 현재 코드의 실측은 **50·53·56·59°**다.
깊이(86·90·94·98°)는 정확히 일치한다. 상체 값만 6° 정도 어긋난다.
갤러리 캡션·타임라인·캡처 이미지에 전부 실측값이 찍히므로 **소개서와 앱을 나란히 보면 차이가 보인다.**
소개서 JPEG는 이미 완성본이라 임의로 고치지 않았다. 그대로 낼지 판단할 것.

---

## 6-C. 2026-08-22 3차 개선 (핵심기능 · 타이포그래피)

### 세트 리포트 (`summary()` / `renderReport()`)
EVIDENCE 섹션 맨 위에 총 회차 · 깊이 도달 · **첫 무너짐 회차** · 챌린지 성공을 한 장으로 모았다.
회차가 끝날 때마다 갱신된다. 흩어져 있던 값을 모아 "세트가 끝나면 증거를 보여준다"는 약속을 닫는 조각.

### 결과 이미지 저장 (`saveReport()`)
1200×812 PNG을 **캔버스에서 직접 합성**한다. 헤더 · 4개 수치 · 타임라인 8칸 · 캡처 4장 · 하단 문구.
`toBlob` + `URL.createObjectURL`로 내려준다(큰 이미지에서 data: URL 한계가 브라우저마다 달라서).
서버가 없으므로 저장도 브라우저 안에서 끝난다. 헤드리스에서 실제 다운로드까지 확인했다(약 200KB).

> 참고: 게시글 사진은 8장이 이미 꽉 차 있다(소개서 5 + SNS 3). 이 PNG는 **유튜브 영상용**으로 쓰는 게 맞다.

### 지난 세트 비교 (`finishSet()`)
`localStorage['rewind.lastSet']`에 마지막 세트 요약을 남기고, 다음 세트가 끝나면
"첫 무너짐 5회차 → 6회차 (+1)"처럼 비교해 준다. **기기 밖으로 나가지 않는다는 서사와 같은 방향**이라
프라이버시 원칙과 충돌하지 않는다. 세트 종료 지점은 두 곳 — 시연 8회 완료, 카메라 15초 비활동.
`setSaved` 플래그로 세트당 한 번만 쓴다. localStorage 접근은 전부 try/catch로 감쌌다.

### 세트 종료 후 결과로 스크롤
심사위원이 스크롤을 내릴 이유를 모를 수 있어, 시연이 끝나면 0.9초 뒤 리포트 카드로 부드럽게 이동한다.
`prefers-reduced-motion`이면 즉시 이동한다.

### 한글 타이포그래피
- **`word-break:keep-all`** — 한글이 어절 중간에서 끊기던 것을 막았다. 체감 차이가 가장 컸다.
- **글꼴 폴백** — `--sans` / `--mono` 토큰으로 정리하고 `Apple SD Gothic Neo`·`Malgun Gothic`·`Noto Sans KR`을
  넣었다. **CDN이 막혀도 한글이 제 모양으로 떨어진다**(검증 환경에서 실제로 막히는 상황이다).
  `--mono`에도 한글 폴백을 넣어 `.mono` 안의 한글이 제각각 렌더되지 않게 했다.
- **자간** — 라틴 기준으로 잡았던 음수 자간(h1 -.045em)이 한글에서 답답해 보여 -.028em으로 풀고
  행간을 1.12 → 1.24로 넓혔다. h2도 같은 방향.
- **숫자 폭 고정** — `tnum`을 폴백 글꼴에도 적용해 자릿수가 바뀔 때 계측값이 떨리지 않게 했다.
- `cdn.jsdelivr.net` preconnect 추가.

---

## 6-D. 팔레트 교체와 소개서 재생성 (2026-08-22 6차)

### 첫 화면 (2026-08-22 7차)
히어로가 텍스트만 쌓여 있어서 스크롤 전까지 제품이 안 보였다. 2단으로 바꾸고
오른쪽에 **실제 계측 도형**을 넣었다.

- 장식 그림이 아니다. `CUR.synth() → CUR.metrics() → draw()`를 그대로 통과시킨다.
  캡션의 각도는 이 앱이 실제로 계산한 값이다. 종목을 바꾸면 도형도 바뀐다
- `draw()`가 다른 캔버스에도 그릴 수 있도록 `cv`/`ctx`를 `const`에서 `let`으로 바꾸고,
  렌더 전후로 바인딩을 스왑한다
- 관절 좌표의 경계 상자를 재서 상자에 맞춰 확대한다. 종목마다 그림이 놓이는 자리가 다르다
- 한 사이클 약 10초로 아주 느리게 움직인다. 측정을 시작하면 멈추고,
  `prefers-reduced-motion`이면 정지 화면
- 모바일(≤1000px)에서는 도형을 200px로 줄여 CTA 아래에 남긴다. 첫 화면에서 제품이 보여야 한다

h1은 2단 레이아웃에 맞춰 `clamp(27px,3.2vw,37px)`로 낮췄다. 이보다 크면 세 줄로 접히고
"남깁니다."가 홀로 남는다.

### 팔레트
| | 이전 | 지금 |
|---|---|---|
| 배경 | `#081130` 로열 네이비 | `#0B0E14` 중립 잉크 |
| 강조 | `#54D6EE` 네온 시안 | `#79B8FF` 블루 |
| 경고 | `#FF6B3D` 핫 오렌지 | `#E8A25E` 앰버 |
| 달성 | `#7CE0B0` 형광 민트 | `#5CCB9B` 세이지 |
| 보조 | `#94A6C9` | `#8A93A3` 중성 회색 |

배경 채도가 높으면 그 위의 색이 전부 들뜬다. 배경을 중립에 가깝게 낮추는 것만으로
같은 강조색도 훨씬 차분해진다. 배경 격자는 선 → **점**(26px)으로 바꿨다.
글로우(버튼 발광·표시등·캔버스 그림자)는 전부 줄이거나 없앴다.
명암비는 전부 5.7:1 이상이다.

### ⚠ 소개서를 다시 뽑기 전에 — 한글 폰트부터 확인할 것
이 개발 환경에는 **한글 폰트가 설치돼 있지 않았다.** `fc-list :lang=ko`에 WenQuanYi(중국어)와
Unifont만 나온다. 그 상태로 렌더한 JPEG는 **한글이 중국어 폰트로 그려진다** — 자형이 미묘하게
어긋나고 자간이 뭉친다. 눈으로는 "폰트가 좀 이상한데" 정도로만 보여서 놓치기 쉽다.

```bash
# Pretendard(본문) · JetBrains Mono(수치) 설치 — raw.githubusercontent.com은 이 환경에서 열린다
mkdir -p /usr/share/fonts/truetype/pretendard && cd $_
B=https://raw.githubusercontent.com/orioncactus/pretendard/main/packages/pretendard/dist/public/static
for w in Regular Medium SemiBold Bold ExtraBold; do curl -sSLo Pretendard-$w.otf "$B/Pretendard-$w.otf"; done
fc-cache -f
```
CSS 스택은 `Pretendard`를 **맨 앞**에 둔다. `Pretendard Variable`은 설치본에 없다.

### 소개서는 16:9 가로 슬라이드다 (2026-08-22 8차)
세로 A4(1240×1754)로 만들었더니 홈글 피드의 가로형 이미지 영역에서 높이에 맞춰
축소돼 글씨가 작아졌다. 경쟁작들은 전부 가로 슬라이드다. **1600×900**으로 다시 짰다.

- 세로 여백이 1518px → 770px로 반 이하가 되므로 본문을 2단으로 재배치하고 타입을 약 0.6배로 줄였다
- 4장의 계측 패널은 세로형이라 가로 슬라이드에서 읽히지 않는다. **세트 리포트**(가로형, 가동범위·피로도 포함)로 교체했다
- 각 페이지가 900px를 넘지 않는지 렌더 시 `scrollHeight`로 검사한다
- 잘린 글자도 함께 검사한다(`scrollHeight > clientHeight`인 말단 요소). 실제로 통계 카드의
  "10명 중 8명"이 50px 고정폭에서 넘쳤다 — 숫자용 mono에 한글이 들어가면 폭이 어긋난다
- 라틴 기준의 강한 음수 자간은 한글에서 글자가 붙어 보인다. h1 -.03em → -.021em,
  h2 -.028em → -.019em, 카드 제목 -.025em → -.014em으로 풀고 행간을 함께 넓혔다

### 소개서 재생성
`deck.html`이 참조하는 `a_live.png` · `a_report.png` · `a_shots.png`는
**앱에서 직접 캡처한 것**이다. 팔레트나 UI가 바뀌면 다시 뽑아야 한다.

```bash
# 1) 앱 화면 3장 (device_scale_factor=2)
#    #screen(최저점 부근) → a_live.png, .panel → a_panel.png, #shots → a_shots.png
# 2) deck.html을 1600x900 뷰포트로 열고 .page 다섯 개를 JPEG(quality 92)로
#    (렌더 전에 각 .page의 scrollHeight <= 900 확인)
```
두 단계 모두 Playwright 스크립트로 돌린다. 5장 각 220~270KB.

---

## 7. 참고 오픈소스 (2026-08-22 검색 결과)

주의: 대부분 **Python + OpenCV 데스크톱 앱**이라 브라우저에 그대로 쓸 수 없다.
알고리즘 아이디어만 참고하고, **코드를 통째로 가져오지 말 것** — 공모전 유의사항 01(본인 창작물)에 걸린다.
MediaPipe 자체(Apache-2.0)를 라이브러리로 쓰는 것은 전혀 문제없다.

| 저장소 | 참고 가치 | 비고 |
|---|---|---|
| `leooooii/PoseEstimation_Squat-system` | 상태머신 설계 (기립→하강→최저→상승) | 우리 구조와 동일한 접근 |
| `rushmash91/Exercise-Counter` | 푸시업·런지·컬 각도 정의 | Python |
| `mansikataria/SquatDetection` | 고관절–무릎 각 임계값 130° 사용례 | 우리는 100°/158° 사용 |
| `Pradnya1208/Squats-angle-detection...` | 각도 기반 교정 피드백 문구 | Python |
| GitHub `pose-estimation` 토픽 (HTML) | 웹캠 body-controlled 게임 사례 — **게임 모드 참고용** | 브라우저에서 동작 |

---

## 8. 이 프로젝트에서 이미 내린 결정 (되돌리지 말 것)

| 결정 | 이유 |
|---|---|
| 모두봄·SignBridge 재사용 안 함 | 유의사항 01 — 타 공모전 수상·발표작 제출 불가. 모두봄은 전국대회 최우수상작이라 검색되면 수상 취소 위험 |
| "AI가 자세를 판정"이 아니라 "3개 각도를 측정" | 통짜 판정은 틀렸을 때 설명 불가. 측정값 노출이 실현가능성 20% 점수에 유리 |
| 온라인 투표 12% 사실상 포기 | 마감 당일 게시 → 하트 확보 시간 없음. 심사위원 80%에 집중 |
| 단일 HTML 유지 | 주최 안내가 ".html 파일". 폴더 배포도 되지만 단일 파일이 가장 안전 |
| 게임화보다 계측 정직성 우선 | 경쟁작 7건 전부 하드코딩 시나리오. 실제 동작이 유일한 차별점 |

---

## 9. 확인이 필요한 항목 (본인 판단 필요)

- 소개서 1장의 통계 2건: 스포츠안전재단 「2015년 스포츠 안전사고 실태조사」 40.5% / "성인 10명 중 8명 홈트 경험"(2020년 보도). **둘 다 조사 시점이 오래됐다.** 출처·연도를 명시해 뒀으나 뺄지 판단할 것
- 시상식 2026-08-29(토) 성남 **오프라인 참석 필수**. 불참 시 수상 취소. 경산에서 왕복 가능한지
- 사진은 게시글당 최대 8장 = 소개서 5 + SNS 인증 3. 정확히 꽉 찬다
